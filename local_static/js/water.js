const FormApp = {
    delimiters: ['[[', ']]'],
    data() {
        return {
            field_name: "HI",    
        }
    },
    methods: {
    }
        
};

Vue.createApp(FormApp).mount('#water')